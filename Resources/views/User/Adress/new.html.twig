<h1>User\Adress creation</h1>

<form action="{{ path('user_addr_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('user_addr') }}">
            Back to the list
        </a>
    </li>
</ul>
