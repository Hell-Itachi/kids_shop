<h1>Template\Attr edit</h1>

<form action="{{ path('attributs_update', { 'templid': templid, 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('attributs', { 'templid': templid }) }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('attributs_delete', { 'templid': templid, 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
<a href="{{ path('attributsvalue_new', { 'id': entity.id }) }}" target="_blank"> New attrvalue </a>