{% extends 'ItcAdminBundle::layout.html.twig' %}
{% block menu %}
{% endblock %}
{% block body %}
{% block styles %}
<style>

.hidden_loader {
       cursor: pointer;
    height: 60px;
    left: -10px;
    opacity: 0;
    position: absolute;
    right: 0;
    top: 10px;
    width: 80px;
    z-index: 5;
}
</style>
{% endblock %}

<form action="{{ path('videogallery_delete', { 'id': entity.id , 'product_id':product_id}) }}" class="delete_videogal" method="post">
    {{ form_widget(delete_form) }}
</form>
<div class="form-title form-title-ins">
        <div class="form-title-content">
                    <div class = 'menu_sys_icon_loader'>
                            <i class="item-edit-icon"></i>
                    </div>
                <div class="item-controls">
                        <a href="#" class="save-item" title="Сохранить" onclick="$('.menu-items-form').submit()"></a>
                        <a href="#" class="del-item" title="Удалить"  onclick="$('.delete_videogal').submit()"></a>
                        <div class="clear"></div>
                </div>
                <span class="title">Видеогалерея</span>
                <span></span>
                <div class="clear"></div>
        </div>
</div><!-- .form-title -->
<br />
<form action="{{ path('videogallery_update', { 'id': entity.id, 'product_id':product_id }) }}" method="post" class="menu-items-form"  style="top:40px; padding-top:0px;" {{ form_enctype(edit_form) }}>  
<div class="menu-item-info">
        <div class="item-info">
                <label for="pid">Название</label>
                {{ form_widget( edit_form.name ) }}
        </div>
</div>
{{ form_rest(edit_form) }}
</form>
<br />
<br />
<br />
{% render 'ItcKidsBundle:Product/KidsProductVideos:index' with{'video_gal': entity.id} %}

{% endblock %}