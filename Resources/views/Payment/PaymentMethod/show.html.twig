<h1>Payment\PaymentMethod</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Company</th>
            <td>{{ entity.company }}</td>
        </tr>
        <tr>
            <th>Acc_num</th>
            <td>{{ entity.accnum }}</td>
        </tr>
        <tr>
            <th>Icon</th>
            <td>{{ entity.icon }}</td>
        </tr>
        <tr>
            <th>Smallicon</th>
            <td>{{ entity.smallIcon }}</td>
        </tr>
        <tr>
            <th>Kod</th>
            <td>{{ entity.kod }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('pay_meth') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('pay_meth_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('pay_meth_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
